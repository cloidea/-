# 版本记录

## v0.6.1

- 制作页底部恢复“仅生成视频 / 生成并上传”双入口。
- 快捷发布弹窗填写标题、标签及双平台选择；确认后复用生成并上传链路，取消不启动任务。
- 发布页继续支持上传已有成片，最终发布仍需用户在网页确认。

## v0.6.0

- 界面改为浅色卡片风格，拆分“制作视频”和“发布视频”，保留稳定处理模块。
- 模板管理、字幕开关、重新配音默认折叠；生成按钮固定在制作页底部。
- 成片结果支持打开及转到发布，发布页支持单独选择已有视频。
- 增加页面切换、输入保留、忙碌状态、成片交接、小窗口和上传继续按钮的界面测试。

## v0.5.0

- 生成前检查字幕词表和读法；日期、小数、百分比统一配音与字幕读法
- WAV完整性和静音检查；缓存有效配音、字幕，失败重试无需全部重做
- 增加“下次重新配音”“刷新模板”“重试失败平台”入口
- 直接上传原视频，不再生成重复发布包；复用已上传页面
- 磁盘容量预检查，成片临时写入并核对真实时长，防止覆盖已有文件
- 损坏模板提示、有效模板超出数量提示，编码兼容奇数分辨率
- Windows单实例保护、退出时等待浏览器关闭、滚动错误日志
- 真实GPT-SoVITS→字幕→FFmpeg测试通过；平台页面交互仍需账号复测

## v0.4.3

- 字幕对齐支持模型词表以外、可找到同读音同声调候选的汉字，字幕原字不变
- 使用真实音频声学概率计算时间戳，不按总时长分摊，不放宽原有匹配检查
- 用参考音频和同音的“轼”实测通过，补充词表缺字与声学概率回归测试

## v0.4.2

- 编辑时保留实际输入框节点，修复抖音占位提示消失后定位超时、标签未填写
- 标签改为整段输入，避免候选弹窗与逐字符输入造成小红书字符错位
- 核对完整正文、#号和标签边界，不能用标题中的数字冒充标签
- 内容错位时恢复准确文本并标记话题未匹配，不虚报话题选择成功
- 增加 `111` 与 `#1#11` 等回归测试；真实平台效果仍待复测

## v0.4.1

- 标题优先填写并核对，不再被正文识别或上传完成提示阻塞
- 正文增加作品简介、data-placeholder 和常见富文本编辑器识别
- 同时出现多个上传完成提示时正确识别
- 复用同平台已有页面和固定登录目录；已有草稿时提示先处理
- 本次修复经离线回归测试，实际页面填写仍需用户复测

## v0.4.0（2026-09-17，待账号实测）

- 两个发布入口接入专用 Edge 窗口，自动上传视频并填写标题、正文和话题
- 登录与验证码通过“继续上传”恢复，支持停止自动操作
- 抖音、小红书分开保存本机浏览器登录状态，加入 Git 排除
- 上传必须检查完成信号；话题未匹配或字段校验失败时明确提示人工处理
- 始终保留最终发布按钮给用户；未找到上传控件时不猜测操作
- 流程单元测试通过；真实平台页面检查超时，账号端到端验证待完成

## v0.3.2（2026-09-17）

- 成片名称改为清晰的中文用途、生成时间和内容摘要组合
- 发布标题为空时自动使用文案开头作为文件名摘要
- 自动移除 Windows 文件名不支持的字符
- 生成完成后在状态栏明确显示本次生成的文件名

## v0.3.1（2026-09-17）

- 新增“发布已有视频”独立入口，无需重新生成配音和视频
- 复用现有发布包、文案复制、上传页打开和文件定位功能
- 已有视频保留真实文件扩展名，避免仅改后缀造成文件格式错误

## v0.3.0（2026-09-17）

- GUI新增发布标题、标签和抖音/小红书平台选择
- 新增“生成并准备发布”流程
- 自动生成视频、封面、发布文案和发布信息文件
- 自动复制标题与标签并打开对应创作者上传页
- 自动在资源管理器中选中待上传视频
- 保留用户最终检查和点击发布，程序不保存账号密码

## v0.2.1（2026-09-17）

- 修复包含阿拉伯数字的文案无法生成强制对齐字幕的问题
- 修复字幕子进程中文错误信息在 Windows 弹窗中显示乱码的问题
- 视频模板选择增加明显边框高亮并记住上次选择
- 生成状态明确显示实际使用的模板名称

## v0.2.0（2026-09-15）

在不改动稳定配音、字幕和合成模块的前提下优化视频模板选择与时长提示。

- 自动读取 `assets/videos/` 中最多 5 个视频模板并生成缩略图
- 支持点击缩略图选择模板，并保留自选本地视频入口
- 根据固定参考音色历史语速显示预计配音时长
- 配音完成后显示实际音频与模板视频时长
- 模板足够长时直接裁剪，不进行循环
- 配音超过模板长度时，由用户确认后才调用现有循环逻辑
- 最终成片时长仍严格以最终 WAV 的真实时长为准

## v0.1.0-mvp（2026-09-15）

第一个最小可用版本。

- GPT-SoVITS 本地胖猫音色配音
- 固定参考音频与匹配参考文本
- 配音内容异常时阻止生成错误字幕
- 已知文案与最终 WAV 的中文字级强制对齐
- 可独立配置的字幕分页与 ASS 烧录
- 根据配音时长自动循环或裁剪基础视频
- 输出 H.264、AAC、yuv420p MP4
- 简洁 Windows 图形界面与桌面启动入口
- 当前确认样片：`output/修复完成_完整样片.mp4`（运行产物，不纳入 Git）
