# v0.5.0 异常处理测试记录

## 已验证

回归套件36项：项目环境33项通过、3项因缺少模型依赖跳过；这3项已在GPT-SoVITS运行环境另行通过（该环境4项测试全通过）。编译检查和Git差异检查通过。

- 真实GPT-SoVITS配音：`苏轼说，增长12.5%，还有20米。`。
- 真实中文模型预检查：`苏轼在2026-09-17说：增长12.5%，还有20米。`。
- 真实强制对齐、ASS和FFmpeg合成：字幕保留“轼”“12.5%”；配音5.72秒，MP4容器5.75秒，约一帧差值。
- 真实FFmpeg短配音1.3秒裁剪、长配音6.3秒循环；输出H.264/AAC，临时文件不残留。
- 注入字幕失败、随后合成失败再重试：配音只生成一次，字幕成功后复用。
- 注入磁盘不足、无声WAV、编码失败；检查在错误时不留下正式成片。
- 引用音频/文本变化使配音缓存失效；重新配音按钮可主动跳过缓存。
- 连续标签、数字标签、错位恢复、词表缺字、时间戳等回归测试。
- GUI启动980×900，新增按钮可见；没有修改或删除用户已有视频。

## 仍需真实账号验证的部分

平台测试目前为模拟控件与状态，不等于抖音、小红书线上验收。登录过期、验证码、话题候选结构、账号限制和网页改版需要在实际账号页面复测。程序暂停或明确提示，不会点击最终发布。

人名、多音字、纯英文与特殊符号不可能仅靠当前中文模型全部正确推断。可映射汉字走同音声学对齐，不可映射内容提前提示改写；不伪造时间戳。漏字重复主要依赖既有对齐置信度检查，不能替代人工试听。

## 复测入口

项目环境运行 `python -m unittest discover -s tests`；其中pypinyin/torch测试另在GPT-SoVITS运行环境执行 `test_alignment_vocabulary.py`。真实音色整链路脚本为 `tests/run_voice_integration.py`，临时音视频在测试完成后自动清理。
